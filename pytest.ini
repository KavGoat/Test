[pytest]
testpaths = tests
# PySide6's offscreen platform plugin does not cope with pytest's faulthandler
# stderr redirection, so turn that plugin off for the whole suite.
addopts = -p no:faulthandler -q
filterwarnings =
    ignore::DeprecationWarning
